import {
  NodeShared,
  PylonMetaResp,
} from "@/declarations/neuron_pylon/neuron_pylon.did";

const SnsNeuronModify = ({
  vector,
  meta,
}: {
  vector: NodeShared;
  meta: PylonMetaResp;
}) => {
  return <div>SnsNeuronModify</div>;
};

export default SnsNeuronModify;
